export const metadata = {
  // Public on purpose: a page that requires a session cannot be where a session starts.
  requireAuth: false,
  pageTitle: 'Warehouseman panel sign in',
  pageTitleKey: 'warehouseman.login.title',
} as const
